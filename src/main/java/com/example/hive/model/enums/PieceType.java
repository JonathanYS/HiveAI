package com.example.hive.model.enums;

/**
 * Enum representing the different types of pieces in the Hive game.
 * Each constant corresponds to a specific piece type with its own movement rules and behavior.
 */
public enum PieceType {
    QUEEN_BEE, ANT, SPIDER, GRASSHOPPER, BEETLE, BLANK
}
